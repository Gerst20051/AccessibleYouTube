/*
 * Eventually, this function will say the given text out loud.
 * Currently it's just a placeholder.
 */
function textToSpeech(text) {
    //console.log('I\'m saying the following: "' + text + '"');
}
